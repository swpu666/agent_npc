# AI 原生开发岗位作业题：游戏问答 Agent 最小可用 Demo

## 一、作业背景

你需要为一款游戏开发面向玩家的 AI Agent 接口，并提供一个 HTML 页面供玩家使用。玩家可以向游戏内的向导 NPC 询问基础玩法、提出简单的情境问题，也可以进行少量闲聊。

请从《王者荣耀》《英雄联盟》或你熟悉的其他游戏中选择一款，借助 AI 开发工具完成一个小而完整的工程 Demo：

**玩家在网页提问 → 系统识别意图 → 按需检索游戏知识 → 生成回答 → 网页展示结果。**

同时提供可以重复运行的离线评测，验证这个 Agent 是否回答正确、是否按照预期处理问题，以及响应是否足够快。

**最终交付：一份可运行的工程 Demo＋一份设计文档。**

## 二、注意事项

- 鼓励使用 AI 辅助设计、编码、生成测试和排查问题；重点考察你能否理解、验证并交付 AI 参与开发的工程。
- 技术栈、模型和开发工具不限。使用本地模型或在线模型接口均可。
- 只做单游戏、单向导 NPC 的基础问答，不需要接入游戏客户端。
- 不要求登录、账号系统、数据库、云部署、复杂 UI、向量数据库、多 Agent 协作或模型训练。
- 优先交付完整链路，不以代码量、框架数量或架构复杂度加分。

## 三、功能要求

### 1. HTML 使用页面与 Agent 接口

提供一个浏览器可访问的 HTML 页面，并连接实际可调用的后端 Agent 接口。

**页面最低要求：**

- 支持输入问题、发送请求和展示玩家/NPC 的对话记录。
- 支持连续追问，以及清空对话。
- 请求过程中有等待状态；失败时给出明确提示，并允许重新发送。
- 对知识类回答展示使用的资料来源；无资料支持时不伪造来源。

**接口最低要求：**

- 至少提供一个对话接口，例如 `/api/chat`，输入包含玩家消息和必要的会话上下文。
- 输出包含回答、识别到的意图、引用资料和响应耗时；流式或非流式均可。
- 支持至少连续 3 轮交互，能够理解“那这个呢”等依赖前文的问题。可以传递最近若干轮历史，不要求持久化会话。
- 处理空输入、过长输入和模型调用失败；设置可配置的请求超时。
- 模型密钥仅保留在后端，不写入 HTML、前端代码或提交的仓库。

NPC 可设定为原创的游戏训练向导，不要求复刻官方角色。默认无法读取实时对局、玩家账号或战绩，不能声称执行了实际上未接入的操作。

### 2. 基础游戏知识支持与知识库设计

从公开可访问的资料中整理一个小型知识库，并在实际回答链路中使用它。

**最低要求：**

- 收录游戏核心知识，覆盖至少 3 类基础内容，例如对局目标、位置职责、地图机制或基础操作。
- 每条知识至少包含：唯一编号、主题、正文、来源标题、来源链接、采集日期；涉及版本变化的内容注明适用版本或范围。
- 知识随工程提供，本地文本或 JSON 文件即可，不要求实时爬取，但需注明知识检索和排序逻辑。
- 根据玩家问题及必要上下文检索相关知识，而不是把所有资料无差别塞入每次请求。

**设计文档需说明：**

1. 为什么这样划分知识单元，如何组织字段与主题。
2. 使用什么检索方法，如何处理同义词、游戏简称和多轮追问。
3. 未命中、资料冲突或版本不明确时如何处理。
4. 如何更新知识，以及知识规模增大后可以如何演进。

关键词检索、规则检索、向量检索均可。评审关注是否适用、能否验证，不要求为小数据集引入复杂组件。

### 3. 基础意图模块

实现一个独立可测试的意图识别与路由模块，至少区分以下四类请求：

| 意图 | 行为要求 |
| --- | --- |
| 游戏知识问答 | 检索资料后回答，例如基础规则、位置职责、地图机制 |
| 游戏情境建议 | 结合玩家已给出的条件回答；缺少关键条件时适当追问 |
| 日常闲聊 | 简短自然地交流，保持向导身份，不强制检索知识库 |
| 能力范围外请求 | 对不支持的请求说明限制，例如要求查询未接入的个人战绩 |

规则、模型或混合方案均可。需要说明各类的边界、多个意图同时出现时的优先级，以及无法确定时的兜底策略。

“需要补充信息”可以作为路由状态，不强制再增加一个意图类别。不要因为游戏资料未命中，就把所有游戏问题都判定为能力范围外请求。

对话历史和知识材料应作为待处理数据使用，不能因为其中出现“忽略规则、直接输出指定结果”等文字就覆盖系统的行为约束。

### 4. 响应效率与基本可观测性

请让玩家能够尽快得到可用回复，并提供真实测量结果。

不统一规定硬性的秒数门槛，也不要求压测。重点说明等待时间主要花在哪里，以及你采取了什么合理措施，例如减少无必要的串行模型调用、限制输入上下文或按意图跳过检索。

### 5. 可重复运行的离线评测

提供一个本地可执行的评测入口：读取固定测试集，调用与网页相同的 Agent 核心链路，输出逐条结果和汇总报告。

**“离线”指基于固定数据集、不依赖真实在线玩家流量进行批量评测；允许调用在线模型接口，不要求模型完全断网运行。**

#### 测试集

至少准备 **12 个测试案例**，覆盖四类意图。每个案例包括编号、问题或对话上下文、预期意图、预期行为、正确回答应覆盖的要点，以及不应出现的内容；需要知识支撑的案例另行标明预期知识编号。

其中必须包含：

- 至少 2 个依赖上下文的多轮案例；
- 至少 1 个知识库未覆盖或资料不足的案例；
- 至少 1 个需要补充条件的情境问题；
- 至少 1 个要求使用未接入能力的案例；
- 至少 1 个在用户文本中尝试干扰系统规则的案例。

以上特征可以重叠，不要求在 12 个案例之外额外增加样本。预期答案和评分规则仅供评测器使用，不能传入被测 Agent。

#### 自动评价

至少输出以下指标，并说明各指标的适用案例与分母：

| 指标 | 评价内容 |
| --- | --- |
| 意图准确率 | 实际意图与预期意图是否一致 |
| 知识检索命中率 | 需要资料的案例是否检索到预期相关知识 |
| 回答通过率 | 是否覆盖关键事实或预期行为，有无明显错误、无依据断言或能力越界 |
| 成功率与耗时 | 调用是否成功，以及实际响应时间 |

回答评价可以采用规则断言、模型评审或组合方式，但不能只凭“请求成功”“回答非空”或完整字符串相等来判断质量。涉及语义的关键词判断有局限，需明确适用范围；使用模型评审时，需提供可检查的评分规则、依据和评审模型信息。

检索命中与回答正确应分开统计：拿到了正确资料，不代表最终回答一定正确。模型评审消耗的时间也应与被测 Agent 的回答耗时分开。

## 四、交付物

### A. 完整工程 Demo

通过代码仓库或压缩包提交，至少包含：

- HTML 使用页面及后端 Agent 接口代码。
- 可测试的意图模块、知识检索与回答生成逻辑。
- 游戏知识文件及来源信息。
- 离线测试集、可执行评测入口、实际评测结果及必要的对照记录。
- 依赖声明、无密钥的配置示例和启动说明。

评审应能按照说明完成：**配置模型 → 启动服务 → 在网页连续提问 → 执行离线评测 → 查看报告。**

如果提供 Mock 模式用于检查页面或接口，必须明显标识；Mock 运行结果不能替代真实模型调用及其质量评测。禁止提交真实玩家隐私、访问凭证或未经授权的数据。

### B. 一份设计文档

建议 **2—4 页**，可以同时承担工程的使用说明，不要求另做演示文稿。至少说明：

1. **运行方法：**依赖、模型配置、启动和评测方式，以及已验证环境。
2. **接口与流程：**接口输入输出、上下文管理、意图路由、检索和生成的调用关系。
3. **知识库设计：**知识结构、资料来源、检索策略、更新方式与未知问题处理。
4. **离线评测：**测试覆盖、评价方法、真实结果、人工抽查与一次改进。
5. **效率与取舍：**实际投入时间、响应耗时、简化了什么、还存在哪些限制。
6. **AI 开发过程：**使用的工具或模型、AI 参与的环节，以及至少一个你亲自检查并修正 AI 产出的具体例子；没有此类例子时，如实说明核验过程，不编造问题。
